import { PanelLayout } from "./components/panels/PanelLayout";

/** Display name used by the desktop application shell. */
export const APP_NAME = "Aria";

/** Root renderer component. */
export default function App() {
  return <PanelLayout />;
}
