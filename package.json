{
  "name": "aria-monorepo",
  "description": "Electron workspace UI with streamed Pi sessions.",
  "homepage": "https://github.com/Aaditri-GlobalTech/aria#Aria",
  "author": "Kumar Rahul Anand",
  "maintainer": "Aaditri GlobalTech",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd app dev",
    "build:backend": "bun run scripts/build-backend.ts",
    "check:backend": "bun run scripts/check-backend.ts",
    "build": "bun run build:backend && bun run --cwd app build",
    "release:linux": "bun run build:backend && bun run check:backend && bun run --cwd app release:linux",
    "release:windows": "bun run build:backend && bun run check:backend && bun run --cwd app release:windows",
    "release": "bun run --cwd app release",
    "start": "bun run --cwd app start",
    "test": "bun run --cwd packages/core test && bun run --cwd packages/host test && bun run --cwd app test",
    "check:browser-smoke": "bun run --cwd app check:browser-smoke",
    "typecheck": "bun run --cwd packages/protocol typecheck && bun run --cwd packages/core typecheck && bun run --cwd packages/host typecheck && bun run --cwd app typecheck",
    "check": "biome check --write --error-on-warnings app packages scripts package.json tsconfig.base.json biome.json && bun run typecheck && bun run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "husky": "9.1.7"
  }
}
